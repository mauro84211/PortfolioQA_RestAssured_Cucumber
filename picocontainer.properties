# PicoContainer configuration file

# Define the container
stepData = com.picocontainer.PicoContainer

# Define the components that the container should manage
components = io.cucumber.picocontainer.CucumberPicoContainer

# Define the glue code that the container should use
glue = Steps

# Define the test runner that the container should use
test=runner = options/CucumberTests.java